import { SubtotalFooterDirective } from './subtotal-footer.directive';

describe('SubtotalFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new SubtotalFooterDirective();
    expect(directive).toBeTruthy();
  });
});
