import { SubtotalBodyDirective } from './subtotal-body.directive';

describe('SubtotalBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new SubtotalBodyDirective();
    expect(directive).toBeTruthy();
  });
});
