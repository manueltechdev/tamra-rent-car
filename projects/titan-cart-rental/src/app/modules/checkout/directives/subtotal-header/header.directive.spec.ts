import { SubtotalHeaderDirective } from './subtotal-header.directive';

describe('HeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SubtotalHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
